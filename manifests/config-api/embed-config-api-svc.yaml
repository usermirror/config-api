apiVersion: v1
kind: Service
metadata:
  name: embed-config-api
  labels:
    app: embed-config-api
    um-component: config-api
spec:
  type: LoadBalancer
  ports:
  - name: http
    targetPort: 8888
    port: 80
  selector:
    app: embed-config-api
